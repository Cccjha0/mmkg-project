# Inference Module

Use this directory for inference-time model loading, preprocessing, postprocessing, and service-facing wrappers.

Recommended next steps:

- add a stable predictor interface
- add artifact loading utilities
- keep training-only dependencies out of runtime paths where possible

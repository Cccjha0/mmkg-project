# ML Module

`ml` contains all machine learning assets for the project:

- `training`: model code, training loops, data readers, and utility scripts
- `inference`: serving-oriented model wrappers and lightweight prediction code
- `artifacts`: checkpoints, metrics, and exported model files
- `configs`: experiment configs and shared defaults

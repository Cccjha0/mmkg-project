seed_list: [1, 2, 3]

training:
  neg_ratio: 10
  corrupt: both
  batch_size: 1024
  lr: 0.001
  epochs: 200
  early_stop_patience: 10

evaluation:
  filtered: true
  chunk_size: 10000
  metrics: [mrr, hits1, hits3, hits10]

embedding:
  d: 256